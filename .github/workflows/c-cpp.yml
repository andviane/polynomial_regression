name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: situation1
      run:  pwd && ls
    - name: mkdir_build
      run:  mkdir -p build
    - name: cd_build
      run:  cd build
    - name: cmake
      run:  ls && cd build && cmake ..
    - name: make
      run:  ls && cd build && make
    - name: run_tests
      run:  ls && cd build && ./tests

      
